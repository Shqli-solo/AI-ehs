apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.javaService.name }}
  labels:
    app.kubernetes.io/name: {{ .Values.javaService.name }}
    app.kubernetes.io/component: java-service
    app.kubernetes.io/part-of: ehs-platform
    {{- include "ehs-helm.labels" . | nindent 4 }}
spec:
  {{- if not .Values.javaService.autoscaling.enabled }}
  replicas: {{ .Values.javaService.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.javaService.name }}
      {{- include "ehs-helm.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Values.javaService.name }}
        app.kubernetes.io/component: java-service
        app.kubernetes.io/part-of: ehs-platform
        {{- include "ehs-helm.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") . | sha256sum }}
        {{- with .Values.javaService.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      serviceAccountName: {{ include "ehs-helm.serviceAccountName" . }}
      securityContext:
        runAsNonRoot: {{ .Values.podSecurity.runAsNonRoot }}
        {{- if .Values.podSecurity.seccompProfile }}
        seccompProfile:
          type: {{ .Values.podSecurity.seccompProfile.type }}
        {{- end }}
      {{- with .Values.javaService.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: java-service
          image: "{{ .Values.global.imageRegistry }}/{{ .Values.javaService.image.repository }}:{{ .Values.javaService.image.tag }}"
          imagePullPolicy: {{ .Values.javaService.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: {{ .Values.podSecurity.allowPrivilegeEscalation }}
            capabilities:
              drop:
                {{- toYaml .Values.podSecurity.capabilities.drop | nindent 16 }}
            {{- if .Values.podSecurity.readOnlyRootFilesystem }}
            readOnlyRootFilesystem: {{ .Values.podSecurity.readOnlyRootFilesystem }}
            {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.javaService.service.targetPort }}
              protocol: TCP
            {{- if .Values.javaService.grpc.enabled }}
            - name: grpc
              containerPort: {{ .Values.javaService.grpc.port }}
              protocol: TCP
            {{- end }}
            {{- if .Values.javaService.extraPorts }}
            {{- toYaml .Values.javaService.extraPorts | nindent 12 }}
            {{- end }}
          {{- if .Values.javaService.livenessProbe }}
          livenessProbe:
            {{- toYaml .Values.javaService.livenessProbe | nindent 12 }}
          {{- end }}
          {{- if .Values.javaService.readinessProbe }}
          readinessProbe:
            {{- toYaml .Values.javaService.readinessProbe | nindent 12 }}
          {{- end }}
          {{- if .Values.javaService.startupProbe }}
          startupProbe:
            {{- toYaml .Values.javaService.startupProbe | nindent 12 }}
          {{- end }}
          resources:
            {{- toYaml .Values.javaService.resources | nindent 12 }}
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: SPRING_PROFILES_ACTIVE
              valueFrom:
                configMapKeyRef:
                  name: {{ include "ehs-helm.configMapName" . }}
                  key: SPRING_PROFILES_ACTIVE
            {{- if .Values.javaService.extraEnv }}
            {{- toYaml .Values.javaService.extraEnv | nindent 12 }}
            {{- end }}
          envFrom:
            - configMapRef:
                name: {{ include "ehs-helm.configMapName" . }}
            {{- if .Values.secrets.enabled }}
            - secretRef:
                name: {{ include "ehs-helm.secretName" . }}
            {{- end }}
          volumeMounts:
            {{- if .Values.javaService.extraVolumeMounts }}
            {{- toYaml .Values.javaService.extraVolumeMounts | nindent 12 }}
            {{- end }}
      {{- with .Values.javaService.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.javaService.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.javaService.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        {{- if .Values.javaService.extraVolumes }}
        {{- toYaml .Values.javaService.extraVolumes | nindent 8 }}
        {{- end }}
